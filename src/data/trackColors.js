// Brand colors for each subject's cards (SQL blue, Python peach, data engineering green).
// These stay the same on every theme, so text placed directly on them must use fixed dark colors
// too (see Learn.jsx and PlaygroundHome.jsx). Full class names are written out so Tailwind can
// find them.
export const trackColors = {
  sql: 'bg-[#cfe3f5]',
  python: 'bg-[#f4e2d0]',
  de: 'bg-[#dcead9]',
}
